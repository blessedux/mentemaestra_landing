{
  "name": "mentemaestra-monorepo",
  "private": true,
  "description": "Monorepo root: Next.js in frontend/, Postgres migrations in backend/, internal docs in docs/.",
  "scripts": {
    "clean": "npm --prefix frontend run clean",
    "dev": "npm --prefix frontend run dev",
    "dev:clean": "npm --prefix frontend run dev:clean",
    "dev:turbo": "npm --prefix frontend run dev:turbo",
    "dev:turbo:clean": "npm --prefix frontend run dev:turbo:clean",
    "booking:availability": "npm --prefix frontend run booking:availability",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint",
    "format": "npm --prefix frontend run format"
  }
}
